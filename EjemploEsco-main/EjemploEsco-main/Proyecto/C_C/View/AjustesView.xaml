<UserControl x:Class="C_C.View.AjustesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <StackPanel Margin="16">
        <TextBlock Text="Ajustes" FontSize="20" FontWeight="Bold" />
        <CheckBox Content="Notificaciones" IsChecked="{Binding NotificacionesHabilitadas}" Margin="0,12,0,0" />
        <CheckBox Content="Modo oscuro" IsChecked="{Binding ModoOscuro}" Margin="0,8,0,0" />
        <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
            <Button Content="Guardar" Command="{Binding GuardarCommand}" Width="120" />
            <Button Content="Cerrar sesión" Command="{Binding CerrarSesionCommand}" Width="120" Margin="8,0,0,0" />
        </StackPanel>
    </StackPanel>
</UserControl>
