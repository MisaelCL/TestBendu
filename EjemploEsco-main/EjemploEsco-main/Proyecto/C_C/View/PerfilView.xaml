<UserControl x:Class="C_C.View.PerfilView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <ScrollViewer>
        <StackPanel Margin="16">
            <TextBlock Text="Tu perfil" FontSize="20" FontWeight="Bold" />
            <TextBlock Text="Nombre" Margin="0,12,0,0" />
            <TextBox Text="{Binding Perfil.Nombre, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Text="Carrera" Margin="0,12,0,0" />
            <TextBox Text="{Binding Perfil.Carrera, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Text="Biografía" Margin="0,12,0,0" />
            <TextBox Text="{Binding Perfil.Biografia, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" Height="100" />
            <Button Content="Guardar" Command="{Binding GuardarPerfilCommand}" Margin="0,16,0,0" Width="120" />
        </StackPanel>
    </ScrollViewer>
</UserControl>
