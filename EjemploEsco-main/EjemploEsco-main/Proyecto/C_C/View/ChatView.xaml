<UserControl x:Class="C_C.View.ChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ListBox ItemsSource="{Binding Mensajes}" DisplayMemberPath="Contenido" />
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,12,0,0">
            <TextBox Text="{Binding MensajeNuevo, UpdateSourceTrigger=PropertyChanged}" Width="300" />
            <Button Content="Enviar" Command="{Binding EnviarMensajeCommand}" Margin="8,0,0,0" />
        </StackPanel>
    </Grid>
</UserControl>
