<Window x:Class="C_C.View.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="C&C - Iniciar sesión"
        Height="450"
        Width="400"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0,4" />
            <Setter Property="Padding" Value="6" />
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Margin" Value="0,4" />
            <Setter Property="Padding" Value="6" />
        </Style>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="0,0,0,16" HorizontalAlignment="Center">
            <TextBlock Text="Clase &amp; Cita" FontSize="26" FontWeight="Bold" />
            <TextBlock Text="Inicia sesión para continuar" FontSize="14" />
        </StackPanel>
        <StackPanel Grid.Row="1">
            <TextBlock Text="Usuario" />
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Text="Contraseña" Margin="0,12,0,0" />
            <PasswordBox x:Name="PasswordBox" PasswordChanged="PasswordBox_OnPasswordChanged" />
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,8,0,0" />
            <Button Content="Ingresar"
                    Command="{Binding LoginCommand}"
                    Margin="0,16,0,0"
                    Height="32" />
            <Button Content="Registrarme"
                    Command="{Binding OpenRegistroCommand}"
                    Margin="0,8,0,0"
                    Height="32" />
        </StackPanel>
    </Grid>
</Window>
