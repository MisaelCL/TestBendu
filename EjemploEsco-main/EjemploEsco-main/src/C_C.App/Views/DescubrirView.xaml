<UserControl x:Class="C_C.App.Views.DescubrirView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Border Padding="24" BorderBrush="#FFCBD5E1" BorderThickness="1" CornerRadius="12" Background="#FFF">
            <StackPanel HorizontalAlignment="Center" Width="400">
                <Image Source="{Binding CurrentCandidate.Perfil.FotoUrl}" Height="200" Stretch="UniformToFill" Margin="0,0,0,12" />
                <TextBlock Text="{Binding CurrentCandidate.DisplayName}" FontSize="24" FontWeight="Bold" Margin="0,0,0,8" />
                <TextBlock Text="{Binding CurrentCandidate.Perfil.Bio}" TextWrapping="Wrap" Margin="0,0,0,12" />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,12">
                    <Button Content="Omitir" Width="120" Command="{Binding SkipCommand}" Margin="0,0,8,0" />
                    <Button Content="Me gusta" Width="120" Command="{Binding LikeCommand}" />
                </StackPanel>
                <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
