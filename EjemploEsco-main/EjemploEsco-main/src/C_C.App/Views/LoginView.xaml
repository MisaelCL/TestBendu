<UserControl x:Class="C_C.App.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <StackPanel Width="360">
        <TextBlock Text="Bienvenido" FontSize="24" FontWeight="Bold" Margin="0,0,0,12" />
        <TextBlock Text="Correo electrónico" />
        <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,12" />
        <TextBlock Text="Contraseña" />
        <PasswordBox x:Name="PasswordBox" PasswordChanged="OnPasswordChanged" Margin="0,4,0,12" />
        <Button Content="Iniciar sesión" Command="{Binding LoginCommand}" Margin="0,4,0,4" />
        <Button Content="Crear cuenta" Command="{Binding RegistrarCommand}" Margin="0,4,0,4" />
        <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap" />
    </StackPanel>
</UserControl>
