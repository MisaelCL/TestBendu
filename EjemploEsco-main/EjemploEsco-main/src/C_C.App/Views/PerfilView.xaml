<UserControl x:Class="C_C.App.Views.PerfilView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <StackPanel Width="400">
        <TextBlock Text="Mi perfil" FontSize="24" FontWeight="Bold" Margin="0,0,0,12" />
        <TextBlock Text="Biografía" />
        <TextBox Text="{Binding Bio, UpdateSourceTrigger=PropertyChanged}" TextWrapping="Wrap" AcceptsReturn="True" Height="100" Margin="0,4,0,12" />
        <TextBlock Text="Ciudad" />
        <TextBox Text="{Binding Ciudad, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,12" />
        <TextBlock Text="Foto (URL)" />
        <TextBox Text="{Binding FotoUrl, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,12" />
        <TextBlock Text="Intereses" />
        <TextBox Text="{Binding Intereses, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,12" />
        <Button Content="Guardar" Command="{Binding GuardarPerfilCommand}" Margin="0,4,0,8" />
        <TextBlock Text="{Binding Mensaje}" Foreground="Green" />
    </StackPanel>
</UserControl>
