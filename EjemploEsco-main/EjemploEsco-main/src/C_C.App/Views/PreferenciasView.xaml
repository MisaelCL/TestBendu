<UserControl x:Class="C_C.App.Views.PreferenciasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <StackPanel Width="300">
        <TextBlock Text="Preferencias" FontSize="24" FontWeight="Bold" Margin="0,0,0,12" />
        <TextBlock Text="Edad mínima" />
        <TextBox Text="{Binding EdadMinima, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,12" />
        <TextBlock Text="Edad máxima" />
        <TextBox Text="{Binding EdadMaxima, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,12" />
        <TextBlock Text="Género" />
        <TextBox Text="{Binding Genero, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,12" />
        <TextBlock Text="Distancia máxima (km)" />
        <TextBox Text="{Binding Distancia, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,12" />
        <Button Content="Guardar" Command="{Binding GuardarPreferenciasCommand}" Margin="0,4,0,8" />
        <TextBlock Text="{Binding Mensaje}" Foreground="Green" />
    </StackPanel>
</UserControl>
