<UserControl x:Class="C_C.App.Views.RegistroView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <ScrollViewer>
        <StackPanel Width="400">
            <TextBlock Text="Crear cuenta" FontSize="24" FontWeight="Bold" Margin="0,0,0,12" />
            <TextBlock Text="Correo" />
            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,12" />
            <TextBlock Text="Contraseña" />
            <PasswordBox x:Name="PasswordBox" PasswordChanged="OnPasswordChanged" Margin="0,4,0,12" />
            <TextBlock Text="Nombre visible" />
            <TextBox Text="{Binding DisplayName, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,12" />
            <TextBlock Text="Fecha de nacimiento" />
            <DatePicker SelectedDate="{Binding DateOfBirth, Mode=TwoWay}" Margin="0,4,0,12" />
            <TextBlock Text="Biografía" />
            <TextBox Text="{Binding Bio, UpdateSourceTrigger=PropertyChanged}" TextWrapping="Wrap" AcceptsReturn="True" Height="80" Margin="0,4,0,12" />
            <TextBlock Text="Ciudad" />
            <TextBox Text="{Binding City, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,12" />
            <TextBlock Text="Foto (URL)" />
            <TextBox Text="{Binding PhotoUrl, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,12" />
            <TextBlock Text="Intereses" />
            <TextBox Text="{Binding Interests, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,12" />
            <StackPanel Orientation="Horizontal">
                <StackPanel Width="120" Margin="0,0,8,0">
                    <TextBlock Text="Edad mínima" />
                    <TextBox Text="{Binding MinAge, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,0" />
                </StackPanel>
                <StackPanel Width="120">
                    <TextBlock Text="Edad máxima" />
                    <TextBox Text="{Binding MaxAge, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,0" />
                </StackPanel>
            </StackPanel>
            <TextBlock Text="Género buscado" Margin="0,8,0,0" />
            <TextBox Text="{Binding TargetGender, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,12" />
            <TextBlock Text="Distancia máxima (km)" />
            <TextBox Text="{Binding MaxDistanceKm, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,12" />
            <Button Content="Registrar" Command="{Binding RegistrarCommand}" Margin="0,4,0,8" />
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap" />
            <TextBlock Text="{Binding SuccessMessage}" Foreground="Green" TextWrapping="Wrap" />
        </StackPanel>
    </ScrollViewer>
</UserControl>
