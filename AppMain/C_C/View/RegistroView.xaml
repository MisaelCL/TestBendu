<Window x:Class="C_C.View.RegistroView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="C_C - Registro" Height="360" Width="420"
        WindowStartupLocation="CenterScreen" WindowStyle="None" AllowsTransparency="True" Background="Transparent">
    <Border CornerRadius="8" Background="#FF1F2933" Padding="16" BorderBrush="#FF334155" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <DockPanel Grid.Row="0" Margin="0 0 0 12">
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                    <Button Content="_" Width="28" Height="24" Click="OnMinimizeClick" Margin="0 0 8 0" />
                    <Button Content="X" Width="28" Height="24" Click="OnCloseClick" />
                </StackPanel>
                <TextBlock Text="Crear cuenta" Foreground="White" FontSize="18" VerticalAlignment="Center" MouseDown="OnDragWindow" />
            </DockPanel>
            <StackPanel Grid.Row="1">
                <StackPanel Margin="0 0 0 12">
                    <TextBlock Text="Correo" Foreground="White" Margin="0 0 0 4" />
                    <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
                <StackPanel Margin="0 0 0 12">
                    <TextBlock Text="Contraseña" Foreground="White" Margin="0 0 0 4" />
                    <PasswordBox x:Name="PasswordBox" PasswordChanged="OnPasswordChanged" />
                </StackPanel>
                <StackPanel Margin="0 0 0 12">
                    <TextBlock Text="Confirmar contraseña" Foreground="White" Margin="0 0 0 4" />
                    <PasswordBox x:Name="ConfirmPasswordBox" PasswordChanged="OnConfirmPasswordChanged" />
                </StackPanel>
                <Button Content="Registrar" Command="{Binding RegistrarCommand}" Height="32" Margin="0 0 0 12" />
                <TextBlock Text="{Binding Error}" Foreground="OrangeRed" TextWrapping="Wrap" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
