<Window x:Class="C_C.View.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:C_C.View"
        mc:Ignorable="d"
        Title="C&amp;C App - Inicio" Height="800" Width="450"
        WindowStyle = "SingleBorderWindow" ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        AllowsTransparency="False">

    <Window.Resources>
        <SolidColorBrush x:Key="FondoPrincipal" Color="#001f3f"/>
        <SolidColorBrush x:Key="AcentoPrincipal" Color="#00bfff"/>
        <SolidColorBrush x:Key="TextoPrincipal" Color="White"/>
        <SolidColorBrush x:Key="FondoContenedor" Color="#EAEAEA"/>
        <Style x:Key="BotonPrincipal" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AcentoPrincipal}"/>
            <Setter Property="Foreground" Value="{StaticResource TextoPrincipal}"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="25">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="LightGray"/>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="LightGray"/>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource FondoPrincipal}">

        <StackPanel VerticalAlignment="Center" Margin="30">

            <TextBlock Text="C&amp;C" 
                       Foreground="{StaticResource TextoPrincipal}" 
                       FontSize="96" 
                       FontWeight="Bold" 
                       HorizontalAlignment="Center" 
                       FontFamily="Impact"/>
            <TextBlock Text="Clase &amp; Cita" 
                       Foreground="{StaticResource TextoPrincipal}" 
                       FontSize="24" 
                       HorizontalAlignment="Center" 
                       Margin="0,-10,0,30"/>

            <Border Background="{StaticResource FondoContenedor}" CornerRadius="20" Padding="25">
                <StackPanel>
                    <TextBlock Text="Matrícula / Correo electrónico:" Margin="0,0,0,5" Foreground="DarkSlateGray"/>
                    <TextBox Margin="0,0,0,15"/>

                    <TextBlock Text="Contraseña:" Margin="0,0,0,5" Foreground="DarkSlateGray"/>
                    <PasswordBox Margin="0,0,0,20"/>
                </StackPanel>
            </Border>

            <Button Content="INICIAR SESION" Style="{StaticResource BotonPrincipal}" Margin="0,25,0,10" Click="Login_Click"/>
            <Button Content="REGISTRARSE" Style="{StaticResource BotonPrincipal}" Click="IrARegistro_Click"/>

            <TextBlock HorizontalAlignment="Center" Margin="0,15,0,0">
                <Hyperlink Foreground="{StaticResource AcentoPrincipal}" TextDecorations="None" Click="IrARegistro_Click">
                    ¿Aún no tienes cuenta?
                </Hyperlink>
            </TextBlock>
        </StackPanel>
    </Grid>
</Window>
